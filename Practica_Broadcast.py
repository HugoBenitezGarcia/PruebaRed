# BUSCANDO, Puerto 4000, id aleatorio 0-9999 (el id mas alto es el host)

import socket
import random

def host():
    sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
    sock.setsockopt(socket.SOL_SOCKET, socket.SO_BROADCAST, 1)

    while

host()

def cliente():



cliente()

def BuscarPartida():
    #Aqui va buscar la ip
BuscarPartida()